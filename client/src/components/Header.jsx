import React from 'react';

const Header = () => {
    return (
        <h2 style={{textAlign:'center',textTransform:'uppercase'}}>
          Expense Tracker  
        </h2>
    );
};

export default Header;